# Day 2

Part 2 is a little more complicated. Need to alter the input zero or one time and check to see if the line is safe or not
So `is_safe` is recursive, with a third argument which serves as the base case, when it's zero, stop the recursion
